# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Logs / temp
*.log
*.tmp

# Node (in case examples are added later)
node_modules/

# Claude Code local, machine-specific settings
.claude/settings.local.json

# dev-loop local state (per-developer, not committed — avoids multi-person merge conflicts)
.claude/dev-state.json
